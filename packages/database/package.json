{
  "name": "@suggester/database",
  "main": "build/index.js",
  "dependencies": {
    "@prisma/client": "^4.11.0",
    "@suggester/configs": "workspace:*"
  },
  "devDependencies": {
    "@trivago/prettier-plugin-sort-imports": "^4.0.0",
    "eslint": "^7",
    "prettier": "^2.8.4",
    "prisma": "^4.11.0"
  },
  "scripts": {
    "build": "prisma generate && tsc",
    "lint": "eslint .",
    "fmt": "prisma format && prettier --write --ignore-path=.eslintignore ."
  }
}
