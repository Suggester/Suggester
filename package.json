{
  "name": "suggester-next",
  "version": "1.0.0",
  "main": "build/src/index.js",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^14.11.2",
    "discord-api-types": "^0.23.1",
    "gts": "^3.1.0",
    "nodemon": "^2.0.13",
    "prisma": "3.2.1",
    "ts-node": "^10.3.0",
    "tsc-alias": "^1.3.10",
    "tsconfig-paths": "^3.11.0",
    "typescript": "^4.0.3"
  },
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "build": "tsc && tsc-alias -s",
    "fmt": "gts fix && prisma format",
    "prepare": "yarn run build",
    "pretest": "yarn run build",
    "posttest": "yarn run lint"
  },
  "dependencies": {
    "@prisma/client": "^3.2.1",
    "reflect-metadata": "^0.1.13",
    "typedi": "^0.10.0"
  }
}
